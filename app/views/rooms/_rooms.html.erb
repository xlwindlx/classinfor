<div class="table-div">
  <% if @rooms.empty? %>
  <p>정보가 없어요</p>
  <% else %>
  <table class="table">
    <thead>
      <tr>
        <th>만든시각</th>
        <th>동</th>
        <th>호</th>
        <th>관리부서</th>
      </tr>
    </thead>
    <tbody>
      <% @rooms.each do |r| %>
      <tr>
        <td><%= r.created_at %></td>
        <td><%= r.building.number %></td>
        <td><%= r.loc %></td>
        <td><%= r.department.name %></td>
      </tr>
      <% end %>
    </tbody>
  </table>
  <% end %>
</div>